---
created_at: 2026-07-03T11:50:27+08:00
updated_at: 2026-07-03T18:33:27+08:00
title: 错题日记 07 · 高等数学
type: mistake
subject: 高等数学
status: review
related_to:
- "[[一元函数积分学]]"
- "[[函数极限连续]]"
- "[[积分学强化笔记]]"
---

# 错题日记 07 · 高等数学

> 配套 1000 题强化第 8 章（一元函数积分学的概念与性质），共 9 题。题 1 熟练度不记、题 11 未做。

| 题 | 主题 | 核心方法 | 状态 |
|---|---|---|---|
| 8.2 | 黎曼和系数 | 中点采样 + 常数项保留 | 🔴 抄录错 |
| 8.3 | ln 型黎曼和 | $\ln(1+u)\sim u$ | 🟡 放缩绕路 |
| 8.4 | 定积分比大小 | 区间再现 + 部分分式 | 🔴 凭感觉 |
| 8.5 | 积分计算 | 拆限 + 三角 + 对称 | 🔴 转换没想起 |
| 8.6 | 常数⟷积分 | 常数化为定积分 | 🟢 方法找到 |
| 8.7 | 复合变上限可导 | 左右极限定义（非链式） | 🟢 正确 |
| 8.8 | 积分方程 | 设 A 为常量解方程 | 🔴 计算符号错 |
| 8.9 | 取整反常积分 | $[x]\leqslant x$ + 比较判别 | 🔴 不等式记反 |
| 8.10 | 反常积分判敛 | 双奇点换元 → 结论大观④ | 🔴 假规则 |
| 8.12 | 反常积分判敛 | $\ln$ 因子 $+\varepsilon$ 比较 | 🟡 方法不熟 |

## [数学] 黎曼和极限——系数里的常数项别丢（强化 8.2）

### Question
$\displaystyle\lim_{n\to\infty}\sum_{k=1}^n\frac{1}{n}\!\left(\frac{2k-1}{2n}+1\right)f\!\left(\frac{2k-1}{2n}\right)$

### My Answer
**我的选择**：把系数抄成 $\frac{k-n/2}{n^2}$，按 $\int_0^1(x-\tfrac12)f\,dx$ 选 B。
**理由**：识破了 $\frac{2k-1}{2n}$ 是中点采样，但抄录把 "+1" 项弄丢，凑成 $(x-\tfrac12)$。

### Correct Answer
**C**：$\int_0^1(x+1)f(x)\,dx$。
**正解**：提 $\frac1n$ 后系数就是 $x_k+1$，$\frac1n\sum(x_k+1)f(x_k)\to\int_0^1(x+1)f\,dx$。

### Error Pattern
抄完题面没回原文核对，凭"应该是 $(x-\tfrac12)f$"的错觉把抄错版当成自己算的。**方法对、答案因抄录错。**

### Core Concept
黎曼和识别（中点采样 $\frac{2k-1}{2n}$）；系数与采样点差 $O(\frac1n)$ 被吸收。

### Fix Plan
抄完题面**用特殊项代入验算**：本题 $k=n$ 时系数应为 $(\frac{2n-1}{2n}+1)\frac1n\to\frac2n$，而 $\frac{k-n/2}{n^2}|_{k=n}=\frac1{2n}$——量级都对不上，立刻能发现抄错。

### 识别信号
$\sum$ 极限里 $\frac{2k-1}{2n}$、$\frac{k}{n}$、$\frac{k}{n}+c$ 都是采样点平移；**系数里跟 $k$ 无关的常数项原样保留，不自作主张改号。**

### 变式自测
系数改为 $\frac1n(\frac{2k-1}{2n}-\tfrac12)$，答：$\int_0^1(x-\tfrac12)f\,dx$。

---

## [数学] 对数型黎曼和——ln(1+u)~u 就是正解（强化 8.3）

### Question
$\displaystyle\lim_{n\to\infty}\sum_{i=0}^{n-1}\ln\!\left[1+\tfrac1n f\!\left(\tfrac in\right)\right]$

### My Answer
**我的选择**：用放缩做出 C，但一直在犹豫能否用 $\ln(1+x)\sim x$。
**理由**：不敢确信等价无穷小在"求和号内 + 每项 $\to0$"情形合法。

### Correct Answer
**C**：$\int_0^1 f(x)\,dx$。**正解就是 $\ln(1+u)\sim u$**（$u=\frac1n f(\frac in)\to0$），不是放缩。

### Error Pattern
对"等价无穷小能否进求和号"没把握，宁可绕放缩。其实**只要替换的是整项且每项 $u\to0$ 一致成立**，就和、积分可交换（Taylor：$\ln(1+u)=u+o(u)$，余项求和 $o(1)$）。

### Core Concept
等价无穷小在黎曼和中的合法性；$\ln(1+u),\ e^u-1,\ (1+u)^\alpha-1\sim u$（$u\to0$）。

### Fix Plan
见到 $\sum\ln(1+\text{小})$、$\sum(e^{\text{小}}-1)$、$\sum[(1+\text{小})^\alpha-1]$ → **第一直觉等价无穷小替换**，再转黎曼和。别绕放缩。

### 变式自测
$\lim_{n\to\infty}\sum_{i=1}^n[e^{\frac1n f(i/n)}-1]$，答：$\int_0^1 f\,dx$。

---

## [数学] 定积分比大小——区间再现求精确关系，不是定性比较（强化 8.4）

### Question
比较 $I_1=\int_0^{2\pi}\frac{\sin x}{x}dx$、$I_2=\int_0^{2\pi}\frac{\sin x}{2\pi-x}dx$、$I_3=\int_0^{2\pi}\frac{\sin x}{x(2\pi-x)}dx$。

### My Answer
**我的选择**：起初选 C（方向对），但提取解析时把 $I_2$ 写成 $=I_1$、答案改写成 B，把自己带偏。
**理由**：用区间再现想"统一分母比较"，没做到底（漏 $\sin(2\pi-t)=-\sin t$ 变号），停在前感觉。

### Correct Answer
**C**：$I_2<I_3<I_1$。
- $I_1>0$：在 $\pi$ 切开，第二段令 $t=x-\pi$，$I_1=\int_0^\pi\frac{\pi\sin x}{x(\pi+x)}dx>0$。
- $I_2=-I_1<0$：令 $t=2\pi-x$，$\sin(2\pi-t)=-\sin t$（**变号不能漏**）。
- $I_3=0$：部分分式 $\frac{1}{x(2\pi-x)}=\frac{1}{2\pi}(\frac1x+\frac{1}{2\pi-x})$，$I_3=\frac{I_1+I_2}{2\pi}=0$。

### Error Pattern
**区间再现只用来"统一分母定性比较"，没用来求精确代数关系。** 正确用法：替换后把两个积分变成"同一个积分 ± 自己"，直接定值（$I_2=-I_1$、$I_3=\frac{I_1+I_2}{2\pi}=0$），不用比较。

### Core Concept
区间再现 $x\to(a+b)-x$；部分分式把 $I_3$ 挂到 $I_1,I_2$；作差保号证符号。

### Fix Plan
**定积分比大小三步**：①区间对称含 $\sin$ → 先试 $x\to(a+b)-x$ 找 $I=\pm I$；②分母是积 → 部分分式挂到已知积分；③判符号 → 作差配分母证保号。**绝不凭感觉。**

### 识别信号
积分区间关于中点对称、被积函数含 $\sin$ 或分母是 $x(a+b-x)$ 形 → 上区间再现 + 部分分式。

### 变式自测
$I=\int_0^\pi\frac{x\sin x}{1+\cos^2 x}dx$，用 $x\to\pi-x$。答：$I=\frac{\pi^2}{4}$。

---

## [数学] 反常积分判敛——"两函数各收敛则积收敛"是假规则（强化 8.10）

### Question
$\displaystyle\int_0^1\frac{1}{x^p|\ln x|^q}\,dx$ 收敛的条件。

### My Answer
**我的选择**：把被积函数拆成 $\frac1{x^p}\cdot\frac1{|\ln x|^q}$，判断"每个因子各自的积分收敛 → 乘积收敛"。
**理由**：套了一条不存在的规则，且**漏看了 $x=1$ 也是奇点**（$\ln x\to0$）。

### Correct Answer
**A**：$p<1$ **且** $q<1$。
**正解**：双奇点（$x=0$ 与 $x=1$）换元统一。令 $\ln x=-t$，$x=e^{-t}$：
$$\int_0^1\frac1{x^p|\ln x|^q}dx=\int_0^{+\infty}\frac{e^{(p-1)t}}{t^q}dt=\underbrace{\int_0^1\frac{e^{(p-1)t}}{t^q}dt}_{\sim\,\int_0^1\frac1{t^q}\,\Rightarrow\,q<1}+\underbrace{\int_1^{+\infty}\frac{e^{(p-1)t}}{t^q}dt}_{\text{结论大观 ④：}p<1\text{ 必收敛}}$$
两段同时收敛 → $p<1$ 且 $q<1$。

### Error Pattern
**用了不存在的"两函数各收敛则积收敛"规则**（反例：$\int_0^1\frac1{x^{1/2}}\cdot\frac1{x^{1/2}}dx=\int_0^1\frac1x$ 发散，但每个因子积分都收敛）+ **漏双奇点**（只看 $x=0$，没看 $x=1$ 处 $\ln x\to0$）。

### Core Concept
双奇点积分必先换元统一；换元后化归 [[积分学强化笔记]] 结论大观的 ④ 型（$\int e^{-\alpha x}\cdot$ 多项式）。

### Fix Plan
**先把假规则从脑子里删掉**。判反常积分：①找全奇点（含 $\ln x$、$\frac1x$、$\frac1{1-x}$ 等）；②奇点 $\geqslant2$ 或被积函数含 $\ln x$ → 令 $\ln x=\pm t$ 换元；③套结论大观定参量范围。

### 变式自测
$\int_0^1\frac1{x^p(1-x)^q}dx$ 的收敛条件。答：$p<1$ 且 $q<1$（双奇点 $x=0$ 由 $p$、$x=1$ 由 $q$，分别套 ①a）。

---

## [数学] 反常积分判敛——ln 因子的 "+ε 比较法"（强化 8.12）

### Question
$\displaystyle\int_0^1\frac{\ln x}{x^p(1-x)^{1-p}}\,dx$ 收敛的条件。

### My Answer
**我的选择**：拆两边、各取极限、求交集——**方法对**，但 $x=0$ 端不会处理 $\ln x$，晕。
**理由**：思路正确（拆奇点 + 比较 + 交集），缺"$\ln x$ 怎么比"的那一招。

### Correct Answer
**A**：$-1<p<1$。
- $x\to0$：$\frac{\ln x}{x^p(1-x)^{1-p}}\sim\frac{\ln x}{x^p}$。因 $\ln x$ 比任何 $x^{-\epsilon}$ 慢，$\lim_{x\to0}\frac{\ln x/x^p}{1/x^{p+\epsilon}}=x^\epsilon\ln x\to0$，由 $\int_0\frac1{x^{p+\epsilon}}$ 收敛（$p+\epsilon<1$）→ 原积分收敛，得 $p<1$。
- $x\to1$：$\ln x\sim -(1-x)$，$\sim -(1-x)^p$，$\int\frac1{(1-x)^{-p}}$ 收敛需 $-p<1\Rightarrow p>-1$。
- 交集：$-1<p<1$。

### Error Pattern
方法对，**$x=0$ 端不会用 "+$\epsilon$ 比较"**。$\ln x$ 不是幂函数，没有干净阶数，要靠 $\epsilon$ 把它夹进幂函数框架。

### Core Concept
**$\ln x$ 比任何幂函数都慢**（$\lim x^\epsilon\ln x=0$）；处理 $\ln$ 因子的固定套路是 $+\epsilon$（或 $-\epsilon$）比较法。关联 [[积分学强化笔记]] 结论大观 ①b（$\int_0^1\frac{\ln x}{x^p}$ 收敛 $0\leqslant p<1$）。

### Fix Plan
判反常积分见到 $\ln x$ 因子：拿 $\frac1{x^{p\pm\epsilon}}$ 作比较，用极限比较法（$\lim=0$ 则被比较收敛 → 原收敛）。$\epsilon$ 取满足 $p\pm\epsilon<1$ 的小正数即可。

### 变式自测
$\int_0^1\frac{\ln x}{x^p}dx$ 收敛条件。答：$p<1$（$0\leqslant p<1$ 是反常情形套 ①b；$p<0$ 时 $x=0$ 非奇点，自然收敛）。

---

## [数学] 定积分正负判断——拆限平移合并 + 对称恒等式（强化 8.5）

### Question
比较 $I_1$（令 $t=x^2$ 后 $=\int_0^{2\pi}\frac{\sin t}{2\sqrt{t}}\,dt$）与 $I_2=\int_{-\pi/4}^{\pi/4}\frac{dx}{1+\sin x}$ 的正负。选 (A)。

### 你的盲点
"两个转换一下子没想到"：① $I_1$ 的**拆限 + 平移合并**；② $I_2$ 的**对称区间恒等式**。

### 正解（两个转换）

**转换 1 · $I_1$ 拆限平移合并**：在 $\pi$ 处拆开 $[0,2\pi]$，第二段令 $t=u+\pi$ 并入前半：

$$I_1=\int_0^{\pi}\sin u\!\left(\frac{1}{2\sqrt{u}}-\frac{1}{2\sqrt{u+\pi}}\right)du\stackrel{(*)}{>}0$$

> $(0,\pi)$ 上 $\sin u>0$，且 $\frac1{\sqrt{u}}>\frac1{\sqrt{u+\pi}}$（$\sqrt{u}$ 更小 → 倒数更大）→ 被积函数恒正。

**转换 2 · $I_2$ 对称恒等式**：

$$\int_{-a}^{a}f(x)\,dx=\int_0^{a}[f(x)+f(-x)]\,dx$$

$$I_2=\int_0^{\pi/4}\!\left[\frac1{1+\sin x}+\frac1{1-\sin x}\right]dx=\int_0^{\pi/4}\frac{2}{\cos^2 x}\,dx=2\tan x\Big|_0^{\pi/4}=2>0$$

| 转换 | 用在哪 | 核心 |
|---|---|---|
| 拆限 + 平移合并 | $I_1$（$[0,2\pi]$ 拆 $\pi$，后半移 $\pi$ 并入前半） | 把"有正有负"变成"恒正"，利用 $\sin(u+\pi)=-\sin u$ |
| 对称恒等式 | $I_2$（$[-a,a]$ 对称区间） | $\int_{-a}^a f=\int_0^a(f(x)+f(-x))\,dx$，化非奇非偶为可算 |

### Core Concept
两个高频套路：
1. **$\sin$ 类积分在 $[0,2\pi]$ 拆 $\pi$ 并半**——$\sin(u+\pi)=-\sin u$ 消掉震荡，配合 $\frac1{\sqrt{u}}$ 的单调性，把变号积分变成保号。
2. **对称区间 $\int_{-a}^a f$**——无论 $f$ 奇偶，**统一用** $\int_0^a(f(x)+f(-x))\,dx$。$f(-x)$ 不是白算的，它正好把分母里的 $\sin x$ 翻成 $-\sin x$，$\frac1{1+\sin x}+\frac1{1-\sin x}=\frac{2}{\cos^2 x}$。

### 识别信号
- 见 $\int_0^{2\pi}\sin(\cdot)\,dx$ → **拆 $\pi$ 平移合并**。
- 见 $\int_{-a}^a$ → **对称恒等式** $f(x)+f(-x)$（不管 $f$ 奇不奇偶不偶）。

### 变式自测
$\int_{-1}^{1}\frac{dx}{1+e^x}$。答：$f(x)+f(-x)=\frac1{1+e^x}+\frac{e^x}{e^x+1}=1$，故 $=\int_0^1 1\,dx=1$。**对称恒等式的经典杀招**——再复杂的 $e^x$、$\sin x$、$\ln x$ 套进去都能化简。

---

## [数学] 常数 ⟷ 定积分的对偶（强化 8.6）

### 方法（与 8.8 配成一对）

| 方向 | 操作 | 适用场景 | 配对题 |
|---|---|---|---|
| **常数 → 积分** | 裸常数 $C$ 写成 $\int_a^b\frac{C}{b-a}dx$ 或 $\frac1{k+1}=\int_0^1 x^k dx$，塞回旁边的积分合并 | 题里冒出孤立常数，直接算卡住 | **8.6** ✅ 方法找到 |
| **积分 → 常数** | 设 $A=\int f^2$，当未知数解方程 | 积分套着未知函数 | **8.8** 🔴 计算错 |

> **心法**：定积分就是个数，数也能伪装成定积分。卡住时想另一边——这是积分学概念性质章的元技能。

---

## [数学] 复合变上限积分在 0 处的可导性 ✅（强化 8.7）

### Question
$f(x)=\int_0^{|\sin x|}e^{t^2}dt$、$g(x)=\int_0^{|x|}\sin t^2\,dt$，判断 $f'(0),g'(0)$ 是否存在 + 奇偶性。选 (B)。

### 你的方法（正确）✅
**不是链式法则**——$|\sin x|$ 在 0 处不可导，链式失效。你用的是**判断导数的左右极限是否相等**：

$$f'_+(0)=\lim_{x\to0^+}\frac{\int_0^{\sin x}e^{t^2}dt}{x}\xrightarrow{\text{洛必达}}e^{\sin^2 0}\cdot\cos 0=1$$

$$f'_-(0)=\lim_{x\to0^-}\frac{\int_0^{-\sin x}e^{t^2}dt}{x}\to e^0\cdot(-\cos 0)=-1$$

| 函数 | $f'_+(0)$ | $f'_-(0)$ | 结论 | 奇偶 |
|---|---|---|---|---|
| $f$ | $1$ | $-1$ | $f'(0)$ **不存在**（左≠右） | 偶（$f_1$ 奇 ∘ $g_1$ 偶） |
| $g$ | $0$ | $0$ | $g'(0)$ 存在（两边都 0） | 偶 |

### 识别信号
变上限 $\int_0^{\phi(x)}$ 且 $\phi$ 含绝对值 → **回导数定义 + 洛必达**（分子求导正好 FTC 给出 $e^{\phi^2}\cdot\phi'$），**别硬套链式**。

---

## [数学] 积分方程——设 A 为常量解方程（强化 8.8）

### Question
$f(x)=3x-\sqrt{1-x^2}\cdot\int_0^1 f^2(x)\,dx$，求 $f(x)$。

### 你的状态
思路对（"把定积分当成常量"✅），**计算错**。

### 正解 + 计算陷阱

```mermaid
flowchart LR
    A["设 A=∫f²"] --> B["代入<br/>f=3x−A√(1−x²)"]
    B --> C["平方展开<br/>9x²−6Ax√(1−x²)+A²(1−x²)"]
    C --> D["逐项积分 0→1"]
    D --> E["解关于 A<br/>的二次方程"]
```

| 展开项 | 积分结果 | 易错点 |
|---|---|---|
| $9x^2$ | $3$ | — |
| $-6Ax\sqrt{1-x^2}$ | $-2A$ | 换元 $u=1-x^2$，**上下限 $u:1\to0$ 翻转添负号** 🔴 你错在这 |
| $A^2(1-x^2)$ | $\frac{2}{3}A^2$ | — |

合：$A=3-2A+\frac{2}{3}A^2\Rightarrow 2A^2-9A+9=0\Rightarrow A=3$ 或 $\frac{3}{2}$。

故 $f(x)=3x-3\sqrt{1-x^2}$ 或 $f(x)=3x-\frac{3}{2}\sqrt{1-x^2}$。

### Fix Plan
**积分方程计算后，用"量纲检查"验算**：$A$ 是 $\int f^2$ 的量级（$f^2$ 积分是个数），解出的 $A=3$ 或 $\frac32$ 都是正数、合理范围。算出 $A<0$ 或 $A>10$ 一定有错。

---

## [数学] 取整函数反常积分——[x]≤x 别记反（强化 8.9）

### Question
$\int_0^n\sqrt{x}\,[\frac{m}{x}]\,dx$（$m,n$ 为正常数）的敛散性。选 (D)。

### 你的错
取整不等式**记反了**：写成 $[x]\geqslant x$（正确是 $\boxed{[x]\leqslant x}$）。导致放缩下界发散、上界收敛，两边夹失效，怔住。

### 正解

| 步骤 | 操作 |
|---|---|
| 换元 $y=m/x$ | $\int_{m/n}^{+\infty}\frac{[y]}{y^{5/2}}\,dy$ |
| 上界 $[y]\leqslant y$ | $\frac{[y]}{y^{5/2}}\leqslant\frac{y}{y^{5/2}}=\frac{1}{y^{3/2}}$ |
| 比较判别法 | $\int_{m/n}^{+\infty}\frac{dy}{y^{3/2}}$ 收敛（$p=\frac{3}{2}>1$）→ **原积分收敛** |
| 结论 | 敛散性与 $m,n$ **均无关** → 选 (D) |

> **非负被积函数判敛：上界收敛即可，不必求下界。** 两边夹只在两边同收敛/同发散时才有定论；一边收敛一边发散恰恰说明你的放缩太松。

### 识别信号
取整 $[x]$ 永远 $\leqslant x$（**地板在底下**）。记不住就画数轴：$[1.5]=1<1.5$，$[2]=2\leqslant2$。

### 变式自测
$\int_1^{+\infty}\frac{[x]}{x^p}\,dx$ 的收敛条件。答：$[x]\leqslant x\Rightarrow\frac{[x]}{x^p}\leqslant\frac1{x^{p-1}}$，收敛需 $p-1>1$ 即 $p>2$。

---

## 相关链接

- [[一元函数积分学]]
- [[函数极限连续]]
- [[积分学强化笔记]]
- [[高数错题日记1]]
